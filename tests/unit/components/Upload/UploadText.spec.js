import { shallowMount } from '@vue/test-utils';
import UploadText from '../../../../src/components/Upload/UploadText';
import setup from '../../setup';

describe('UploadText ', () => {
	it('', () => {
		// eslint-disable-next-line no-unused-vars
		const wrapper = shallowMount(UploadText, {
			...setup
		});
	});
});
